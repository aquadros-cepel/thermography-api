# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
services:
  postgresql:
    extends:
      file: ./postgresql.yml
      service: postgresql
  # Kafka service - DISABLED for development
  # Uncomment the lines below to re-enable Kafka
  # kafka:
  #   extends:
  #     file: ./kafka.yml
  #     service: kafka
